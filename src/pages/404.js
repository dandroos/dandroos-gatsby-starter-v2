import * as React from "react"
import PageWrapper from "../components/PageWrapper"

const NotFoundPage = () => (
  <PageWrapper seo={{ title: "Page not found" }}></PageWrapper>
)

export default NotFoundPage
